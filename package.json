{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "now-build": "lerna bootstrap",
    "now-start": "lerna run prod --scope report-bot",
    "deploy:report-bot": "now --dotenv=./packages/report-bot/stage.env --name=report-bot --public"
  },
  "devDependencies": {
    "lerna": "^3.3.1",
    "tslint": "^5.11.0",
    "typescript": "^3.0.3"
  }
}
